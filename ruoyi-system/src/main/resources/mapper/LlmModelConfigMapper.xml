<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.LlmModelConfigMapper">

    <select id="selectModelList" resultType="com.ruoyi.system.knowledge.domain.LlmModelConfig">
        select
            model_id as modelId,
            model_name as modelName,
            display_name as displayName,
            provider,
            api_key as apiKey,
            endpoint,
            temperature,
            max_tokens as maxTokens,
            status,
            order_num as orderNum,
            remark,
            create_by as createBy,
            create_time as createTime,
            update_by as updateBy,
            update_time as updateTime
        from llm_model_config
        where status = '0'
        order by order_num, model_id
    </select>

    <select id="selectModelById" parameterType="Long" resultType="com.ruoyi.system.knowledge.domain.LlmModelConfig">
        select
            model_id as modelId,
            model_name as modelName,
            display_name as displayName,
            provider,
            api_key as apiKey,
            endpoint,
            temperature,
            max_tokens as maxTokens,
            status,
            order_num as orderNum,
            remark,
            create_by as createBy,
            create_time as createTime,
            update_by as updateBy,
            update_time as updateTime
        from llm_model_config where model_id = #{modelId}
    </select>

</mapper>
