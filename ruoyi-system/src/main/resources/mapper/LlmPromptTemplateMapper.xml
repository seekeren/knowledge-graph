<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.LlmPromptTemplateMapper">

    <select id="selectPromptList" resultType="com.ruoyi.system.knowledge.domain.LlmPromptTemplate">
        select
            prompt_id as promptId,
            prompt_name as promptName,
            prompt_type as promptType,
            content,
            language,
            status,
            order_num as orderNum,
            remark,
            create_by as createBy,
            create_time as createTime,
            update_by as updateBy,
            update_time as updateTime
        from llm_prompt_template
        where status = '0'
        order by order_num, prompt_id
    </select>

    <select id="selectPromptById" parameterType="Long" resultType="com.ruoyi.system.knowledge.domain.LlmPromptTemplate">
        select
            prompt_id as promptId,
            prompt_name as promptName,
            prompt_type as promptType,
            content,
            language,
            status,
            order_num as orderNum,
            remark,
            create_by as createBy,
            create_time as createTime,
            update_by as updateBy,
            update_time as updateTime
        from llm_prompt_template where prompt_id = #{promptId}
    </select>

</mapper>
